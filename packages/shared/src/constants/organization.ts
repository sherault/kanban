/** Shortest query accepted by the organization member search. */
export const MIN_MEMBER_SEARCH_LENGTH = 3;

/** Longest query accepted — the RFC 5321 maximum length of an email address. */
export const MAX_MEMBER_SEARCH_LENGTH = 320;
